# AUTO-GENERATED by scripts/generate-compose.py — do not edit by hand.
# Regenerate with: python3 scripts/generate-compose.py
#
# Starts one container per local session (Claude or Codex).
# Each container runs mcp-server + session-loop under s6-overlay.

services:
  session-openclaw:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-openclaw
    restart: always
    environment:
      SESSION_NAME: "openclaw"
      TELEGRAM_THREAD_ID: "202"
      WORKDIR: "/root/.openclaw/workspace"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/.openclaw/workspace:/root/.openclaw/workspace
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-main:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-main
    restart: always
    environment:
      SESSION_NAME: "main"
      TELEGRAM_THREAD_ID: "213"
      WORKDIR: "/root"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root:/root
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-itops-dev:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-itops-dev
    restart: always
    environment:
      SESSION_NAME: "itops-dev"
      TELEGRAM_THREAD_ID: "185"
      WORKDIR: "/root/itops-agents"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/itops-agents:/root/itops-agents
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-voice:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-voice
    restart: always
    environment:
      SESSION_NAME: "voice"
      TELEGRAM_THREAD_ID: "201"
      WORKDIR: "/root/openclaw-3cx"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/openclaw-3cx:/root/openclaw-3cx
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-relay:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-relay
    restart: always
    environment:
      SESSION_NAME: "relay"
      TELEGRAM_THREAD_ID: "183"
      WORKDIR: "/root/relay"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/relay:/root/relay
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-cliproxy:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-cliproxy
    restart: always
    environment:
      SESSION_NAME: "cliproxy"
      TELEGRAM_THREAD_ID: "187"
      WORKDIR: "/root/cliproxyapi"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/cliproxyapi:/root/cliproxyapi
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-claude-runner:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-claude-runner
    restart: always
    environment:
      SESSION_NAME: "claude-runner"
      TELEGRAM_THREAD_ID: "189"
      WORKDIR: "/root/.openclaw/extensions/claude-runner"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/.openclaw/extensions/claude-runner:/root/.openclaw/extensions/claude-runner
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-oref-native:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-oref-native
    restart: always
    environment:
      SESSION_NAME: "oref-native"
      TELEGRAM_THREAD_ID: "200"
      WORKDIR: "/root/.openclaw/workspace/skills/oref-native"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/.openclaw/workspace/skills/oref-native:/root/.openclaw/workspace/skills/oref-native
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-clawdbot:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-clawdbot
    restart: always
    environment:
      SESSION_NAME: "clawdbot"
      TELEGRAM_THREAD_ID: "456"
      WORKDIR: "/root/clawdbot"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/clawdbot:/root/clawdbot
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-openclaw-core:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-openclaw-core
    restart: always
    environment:
      SESSION_NAME: "openclaw-core"
      TELEGRAM_THREAD_ID: "495"
      WORKDIR: "/root/.openclaw"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/.openclaw:/root/.openclaw
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-mybill:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-mybill
    restart: always
    environment:
      SESSION_NAME: "mybill"
      TELEGRAM_THREAD_ID: "2595"
      WORKDIR: "/root/mybill"
      ALLOWED_USERS: "6831389652"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/mybill:/root/mybill
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-tala-planner:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-tala-planner
    restart: always
    environment:
      SESSION_NAME: "tala-planner"
      TELEGRAM_THREAD_ID: "4067"
      WORKDIR: "/root/tala-planner"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/tala-planner:/root/tala-planner
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-mikrotik:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-mikrotik
    restart: always
    environment:
      SESSION_NAME: "mikrotik"
      TELEGRAM_THREAD_ID: "6252"
      WORKDIR: "/root/mikrotik"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/mikrotik:/root/mikrotik
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-codex:
    image: relay-session-codex:latest
    build:
      context: .
      dockerfile: codex-session.Dockerfile
    container_name: relay-session-codex
    restart: always
    environment:
      SESSION_NAME: "codex"
      TELEGRAM_THREAD_ID: "8542"
      WORKDIR: "/root"
      SESSION_TYPE: "codex"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Codex credentials and config
      - /root/.codex:/root/.codex
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root:/root
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "codex"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-copilot:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-copilot
    restart: always
    environment:
      SESSION_NAME: "copilot"
      TELEGRAM_THREAD_ID: "8928"
      WORKDIR: "/root/.relay-copilot/workspace"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/.relay-copilot/workspace:/root/.relay-copilot/workspace
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  session-remote-desktop:
    image: relay-session:latest
    build:
      context: .
      dockerfile: session.Dockerfile
    container_name: relay-session-remote-desktop
    restart: always
    environment:
      SESSION_NAME: "remote-desktop"
      TELEGRAM_THREAD_ID: "10170"
      WORKDIR: "/root/meshcentral"
    volumes:
      # Shared queue between bot and session containers
      - relay-queue:/tmp
      # Relay env file for the MCP bridge only; do not inject bot creds into Claude/Codex
      - /root/relay/.env:/root/relay/.env:ro
      # Claude credentials and global state (directory + auth file)
      - /root/.claude:/root/.claude
      - /root/.claude.json:/root/.claude.json
      # Docker socket — allows session to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Project working directory
      - /root/meshcentral:/root/meshcentral
    depends_on:
      - relay
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "claude"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  relay-queue:
    external: true
