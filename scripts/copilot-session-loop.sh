#!/bin/bash
# copilot-session-loop.sh — NOT USED
# The copilot topic uses a standard Claude session with mcp-copilot as a tool.
# This script is kept as a placeholder. See mcp-copilot/server.ts for the MCP wrapper.
echo "This script is deprecated. The copilot topic uses a regular Claude session."
echo "Add mcp-copilot to the session's .mcp.json to enable copilot tools."
exit 0
