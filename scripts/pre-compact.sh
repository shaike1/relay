#!/bin/bash
# Pre-compact hook: runs before context window compaction
# Prints a reminder to Claude to use caveman mode after compaction
echo "CONTEXT COMPACTION: Resume in caveman mode. Be terse. Drop articles/filler. Fragments OK."
